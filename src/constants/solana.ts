/**
 * Solana mint addresses and protocol constants.
 * Single source of truth — never hardcode these inline.
 */

/** Native SOL wrapped mint address (44 characters). */
export const SOL_MINT = 'So11111111111111111111111111111111111111112';
